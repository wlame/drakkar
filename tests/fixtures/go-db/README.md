Go-written Drakkar DB fixtures for cross-backend interop tests.

Generated by `just gen-db-fixtures` in the drakkar-go repo
(internal/crossbackend/gen) through the REAL recorder/cache engines.
Worker `go-fixture`, cluster `main`; see the generator's
package comment for the exact row contract the consuming tests rely on.
Regenerate + recommit after any schema or encoding change.
